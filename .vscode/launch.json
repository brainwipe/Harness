{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to url with files served from root",
            "type": "chrome",
            "request": "attach",
            "port": 9222,
            "url": "https://localhost:8080",
            "webRoot": "${workspaceFolder}"
        }
    ]
}